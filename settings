# NordVPN: 1 to install, 0 to skip
nordvpn=0

# iTerm2 profile: 1 to install, 0 to skip
# Configures natural editing key mapping, powerlevel compatible fonts and
# makes the right option key to function as Alt/Esc+ key
iterm_profile=1

# Oh My Zsh! plugins
ohmyzsh_plugins='git history fzf fasd npm docker docker-compose zsh-autosuggestions'

# Python version (without the patch number) to be configured as the global default.
# Will install the latest patch for the specified version.
default_python=3.8
